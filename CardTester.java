/**
 * This is a class that tests the Card class.
 */
public class CardTester {

	/**
	 * The main method in this class checks the Card operations for consistency.
	 *	@param args is not used.
	 */
	public static void main(String[] args) {
		Card card1 = new Card();
		/* You need to make sure write some code to
		 * test your work. Good tests would be:
		 * Create three Cards, two that are the same and
		 * one that is different. Then print out the
		 * values of suit(), rank(), and pointValue() of
		 * one of the cards, or all of the cards, to make
		 * sure they are correctly initialized. Then print
		 */
		/* *** TO BE IMPLEMENTED IN ACTIVITY 1 *** */
	}
}
